# ไฟล์ที่ไม่ต้องขึ้น GitHub
node_modules/
.env
*.log
.DS_Store
Thumbs.db
desktop.ini

# ไฟล์ GAS (deploy ผ่าน Apps Script โดยตรง ไม่ใช่ GitHub)
gas_code.js

# ไฟล์ dev tools
.claude/
*.jsonl

# ไฟล์ต้นฉบับโลโก้ (PDF) — เก็บไว้ในเครื่องพอ
# ไม่ต้องขึ้นเว็บ เพราะรวมกัน 2.6 MB และแอปไม่ได้ใช้
assets/*.pdf
