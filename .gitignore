# ไฟล์ที่ไม่ต้องขึ้น GitHub
node_modules/
.env
*.log
.DS_Store
Thumbs.db
desktop.ini

# gas_code.js ตั้งใจ track ไว้ใน repo (source of truth + ประวัติการแก้) — deploy ผ่าน clasp ไม่ใช่ Pages
# (เคยมีบรรทัด ignore แต่ไฟล์ถูก track อยู่แล้ว ทำให้เข้าใจผิด — เอาออกตามข้อ 13 รายงานปรับปรุง)

# ไฟล์ dev tools
.claude/
*.jsonl

# ไฟล์ต้นฉบับโลโก้ (PDF) — เก็บไว้ในเครื่องพอ
# ไม่ต้องขึ้นเว็บ เพราะรวมกัน 2.6 MB และแอปไม่ได้ใช้
assets/*.pdf
